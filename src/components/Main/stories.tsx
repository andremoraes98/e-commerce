import { Meta, StoryObj } from '@storybook/react'
import Main from './'

export default {
  title: 'Main',
  component: Main,
  parameters: {
    layout: 'fullscreen'
  }
} as Meta<typeof Main>

export const Default: StoryObj = {}
